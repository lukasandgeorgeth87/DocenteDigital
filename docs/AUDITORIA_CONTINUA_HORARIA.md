# Auditoría continua horaria — DocenteDigital

Registro acumulativo complementario al estado maestro. No constituye certificación de lanzamiento ni reemplaza `AUDITORIA_MAESTRA_INTEGRAL_V2.md`, `ADENDA_AUDITORIA_EJECUTABLE_V3.md`, `AUDITORIA_SIMPLICIDAD_USO_V4.md`, `AUDITORIA_PRELANZAMIENTO_V5.md` ni `NUCLEO_IA_DOCENTEDIGITAL.md`.

## Pruebas acumuladas

| ID | Módulo | Entrada | Esperado | Obtenido antes | Estado / Severidad | Causa raíz | Corrección | Evidencia posterior | Riesgo / gate V5 |
|---|---|---|---|---|---|---|---|---|---|
| AUD-DOCX-DATA-022 | Exportación DOCX / integridad institucional | Descargar una unidad o sesión sin `teacherName` y/o `schoolName` registrados | No inventar identidad institucional; omitir datos ausentes o dejarlos claramente pendientes | El OOXML agregaba siempre `Docente · Institución Educativa` mediante valores fallback, pudiendo aparecer como identidad real en el archivo exportado | PASA tras corrección · S2 · PARCIALMENTE FUNCIONAL | `docx-export-v29.js` construía un párrafo final con `state.teacherName||'Docente'` y `state.schoolName||'Institución Educativa'` | El párrafo de identidad solo se incorpora cuando existen valores reales no vacíos; no se alteran documentos históricos guardados | Commit `0d6e22fdbcc5ceb367ab101e277919e5eaa4a04a`; Vercel production READY; raíz HTTP 200; `docx-export-v29.js` HTTP 200 y ya usa `filter(Boolean)` + `identityXml` condicional | Reduce riesgo de dato institucional inventado. La exportación sigue PENDIENTE de apertura física en Word/PDF, impresión, caracteres quechua, tablas, imágenes y prueba de 20 documentos exigida por V5. |
| AUD-PLAN-HOR-023 | Horario / integridad de datos / secuenciación | Primer ingreso sin subir ni confirmar horario | Mantener el horario como dato pendiente; si el docente decide continuar, usar solo una distribución provisional claramente identificada y sin horas/áreas institucionales inventadas | `schedule-v3.js` inicializaba automáticamente un horario completo lunes-viernes con horas y áreas concretas y lo rotulaba `Horario guardado del docente`; nuevas unidades podían secuenciarse con ese dato ficticio | PASA tras corrección técnica · S1 · PARCIALMENTE FUNCIONAL | El horario modelo se usaba como estado inicial y `unitSessionMode` comenzaba en `schedule`, convirtiendo una plantilla en aparente dato real | Se añadió `schedule-integrity-v62.js`: migra únicamente el horario legado automático exacto a `Sin horario confirmado`, deja los días vacíos y cambia la distribución provisional a 2 sesiones/día; conserva horarios importados, configurados o modelos elegidos explícitamente | Commits `f67740984c1a781aa70941e17d5a5dbb699f0e8d` y `326342ba945ceeddc85eeb16f78fbf3ed9412bd3`; Vercel production READY; raíz HTTP 200; `schedule-integrity-v62.js` HTTP 200 con MIME JavaScript | Reduce error silencioso y evita que una plantilla se convierta en hecho institucional. Sigue pendiente prueba física de importación DOCX, edición móvil, persistencia tras cierres e interrupciones y comprobación E2E Unidad→Sesiones. |
| AUD-EIB-DATA-024 | Perfil lingüístico / EIB / integridad de configuración | Primer ingreso o estado legado sin haber confirmado EIB ni lengua originaria | Mantener la lengua originaria como `Ninguna`/pendiente hasta confirmación expresa; no heredar una variedad como hecho institucional | El prototipo base inicializaba `state.quechuaVar='Quechua Collao'`. Aunque la capa EIB posterior ya exigía confirmación, el estado interno podía conservar temporalmente una variedad no confirmada y quedar disponible para módulos legado | PASA tras corrección técnica · S2 · PARCIALMENTE FUNCIONAL | Valor histórico por defecto en `app.js` anterior a la arquitectura actual de perfil lingüístico | `config-state-guard-v42.js` ahora limpia exclusivamente el valor legado `Quechua Collao` cuando `linguisticMode` aún no fue confirmado, fija `Ninguna` y conserva selecciones EIB ya confirmadas; el flujo sigue exigiendo EIB/monolingüe y lengua pertinente | Commit `17bd0777668cbebc1a71f830bc3ce58acbf3ff4e`; Vercel production READY; raíz HTTP 200; `config-state-guard-v42.js` HTTP 200 con la migración; `schedule-prompt-v6.js` confirma que la guardia se carga antes de `linguistic-profile-v26.js` y generadores semánticos | Reduce riesgo de contaminación silenciosa en materiales/planificación legado. Sigue pendiente prueba E2E real EIB→materiales/sesiones y cambio EIB→monolingüe en dispositivos reales, además de validación oficial de denominaciones lingüísticas cuando se use normativa. |
| AUD-STO-RESET-025 | Persistencia / recuperación / seguridad de borrado | Pulsar `Restablecer prototipo` con unidades/sesiones/configuración guardadas | Confirmar la acción y conservar una vía real de recuperación antes de borrar definitivamente, conforme a V4/V5 | `resetDemo()` eliminaba directamente `docenteDigitalPrototype` tras una sola confirmación y recargaba; no existía papelera, backup del estado principal ni opción de deshacer | PASA tras corrección técnica · S1 · PARCIALMENTE FUNCIONAL | El restablecimiento del prototipo se implementó como borrado irreversible de la única fuente local principal | `storage-recovery-v26.js` ahora intercepta el restablecimiento, crea una copia local previa; si el backup falla cancela el borrado; tras reiniciar ofrece `Restaurar` o `Descartar copia` con confirmación para descarte definitivo | Commit `47d956ec5ae86e5f8237889f7993df5baca6f407`; Vercel production READY; raíz HTTP 200; `storage-recovery-v26.js` HTTP 200 y contiene `RESET_BACKUP_KEY`, wrapper recuperable y UI de restauración | Reduce pérdida accidental en el prototipo, pero NO equivale a backup productivo: sigue siendo `localStorage` del mismo dispositivo. V5 mantiene PENDIENTE backend, backup/restauración real, pruebas de cierre/interrupción y aislamiento multiusuario. |
| AUD-UX-SEM-026 | Unidad/Proyecto / V4 / comprensión semántica visible | Escribir una descripción libre en `Idea o contexto de partida` | Analizar internamente el contexto y mostrar solo guía útil en lenguaje docente; no exponer palabras clave ni mecánica técnica como parte del flujo principal | `context-keywords-v19.js` mostraba `🔑 Palabras clave detectadas` y repetía los términos extraídos en el selector de propuestas, haciendo visible una heurística interna y reforzando una experiencia basada en keywords | PASA tras corrección técnica · S3 · PARCIALMENTE FUNCIONAL | Una capa histórica de apoyo semántico mezclaba procesamiento interno con feedback visible al usuario | Se mantiene la extracción interna para compatibilidad, pero la caja visible ahora solo ofrece una ayuda breve en lenguaje natural y elimina `ddProposalKeywords`; no se muestran porcentajes, keywords ni telemetría técnica | Commit `1d756d7fe708808ea50b23f074c446ed90cba2bb`; Vercel production READY; raíz HTTP 200; `context-keywords-v19.js` HTTP 200 y ya no contiene la UI `Palabras clave detectadas` | Mejora V4 y evita inducir al usuario a escribir para el algoritmo. La comprensión semántica real sigue PARCIAL: la capa de keywords permanece como apoyo interno y V5 mantiene pendiente IA semántica real, pruebas robustas y usuarios reales. |
| AUD-STO-DEL-027 | Persistencia / borrado de Unidad-Proyecto | Eliminar una unidad/proyecto guardado | Confirmación + recuperación antes de descarte definitivo; una eliminación accidental no debe ser irreversible | `deleteUnit(id)` eliminaba la unidad directamente de `state.units`, guardaba el nuevo estado y no ofrecía papelera, deshacer ni recuperación | PASA tras corrección técnica · S1 · PARCIALMENTE FUNCIONAL | El borrado individual seguía usando la implementación original irreversible aunque el restablecimiento general ya había sido protegido | `storage-recovery-v26.js` ahora guarda una copia local de la unidad antes del borrado; si el backup falla cancela la operación; tras eliminar ofrece `Restaurar` o `Descartar`, y la restauración reincorpora solo la unidad eliminada sin sobrescribir otros cambios posteriores | Commit `8a2fc8e5e3db97054320ac17e8559f903067178c`; Vercel production READY; raíz HTTP 200; `storage-recovery-v26.js` HTTP 200 y contiene `DELETE_BACKUP_KEY`, wrapper de `deleteUnit` y UI de restauración | Reduce pérdida irreversible en el prototipo. Sigue sin equivaler a papelera/backend multiusuario productivo; resta probar físicamente cierre/reapertura, múltiples borrados y restauración en dispositivos reales. |
| AUD-PED-TIT-029 | Unidad/Proyecto / títulos naturales / observación breve | `observan bastantes moscas en la fruta` | Interpretar la acción observada y formular títulos naturales sin copiar el cuantificador circunstancial como si fuera el tema; conservar el hecho original sin inventar problema, causa o finalidad | La capa V44 retiraba `observan`, pero trataba `bastantes moscas en la fruta` como tema, generando `Observamos bastantes moscas en la fruta`, `Conocemos más sobre bastantes moscas...` y `Descubrimos bastantes moscas...` | PASA defensa técnica · S3 · PARCIALMENTE FUNCIONAL | El normalizador de observaciones separaba el verbo del foco, pero no distinguía cuantificadores circunstanciales iniciales del núcleo temático | `title-context-v38.js` V45 conserva el texto original en el perfil y, solo para construir el título de una observación simple, retira cuantificadores iniciales (`bastante(s)`, `mucho/a(s)`, `varios/as`, `algunos/as`, `unos/as`) para trabajar con el foco `moscas en la fruta` | Commit `a5090b2f14e6d40fe4983d4c49288a7794a1f6fa`; GitHub check `Vercel: success`; producción `title-context-v38.js` HTTP 200 y sirve V45 con `OBSERVATION_QUANTITY`. El endpoint administrativo de Vercel para confirmar literalmente `READY` devolvió 403 por alcance de autenticación, por lo que esa evidencia queda PENDIENTE en esta ejecución | Mejora naturalidad y evita copia mecánica. No demuestra comprensión semántica general ni prueba física móvil. Mantener pendiente validación de situación→reto→producto para esta entrada y casos no codificados. |
| AUD-RUN-MOD-030 | Carga dinámica / errores silenciosos / estabilidad | Un módulo dinámico responde HTTP 200 pero lanza un error JavaScript durante su ejecución | Detectar el fallo, registrar el módulo afectado y advertir al usuario antes de crear/guardar/descargar, no considerar “cargado” solo porque el archivo llegó por red | El cargador solo manejaba `script.onerror`; un módulo con error de ejecución podía disparar `onload`, continuar con la cadena y dejar una función parcialmente rota sin aviso visible | PASA defensa técnica · S2 · PARCIALMENTE FUNCIONAL | El loader equiparaba carga de red exitosa con ejecución válida y no escuchaba errores globales atribuibles a los módulos cargados | `schedule-prompt-v6.js` ahora escucha `window.error`, identifica el archivo por `filename/src`, lo registra en `ddModuleLoadFailures` y muestra la barra de fallo existente cuando el error pertenece a un módulo de la lista; no atribuye promesas rechazadas a un módulo sin evidencia | Commit `04ae6e3f1d9a24b0fe9ae91b136ed893c5b20d00`; producción `/schedule-prompt-v6.js` HTTP 200 y sirve `rememberFailure` + listener global; raíz pública HTTP 200. La consulta administrativa `list_deployments` de Vercel continúa bloqueada por 403 de alcance, por lo que `READY` literal queda PENDIENTE y no se simula | Reduce errores silenciosos, pero no sustituye pruebas E2E ni monitoreo productivo. Sigue pendiente provocar controladamente un error de módulo en entorno de prueba, verificar recuperación y contar con observabilidad/backend real según V5. |
| AUD-SES-SRC-031 | Sesiones / trazabilidad / anti-invención | Entrar a `Crear mi sesión` sin haber creado ninguna unidad/proyecto real y pulsar generar | Bloquear la generación y exigir una unidad/proyecto + actividad programada reales; no usar ejemplos internos como datos del usuario | `app.js` llenaba el selector con `Ejemplo: Proyecto Cuidamos la Pachamama` y actividades fijas (`Medimos espacios para organizar nuestra feria`, `Dialogamos sobre saberes de nuestra comunidad`); `selectedActivity()` y `buildSession()` podían convertir esos fallbacks en una sesión aparentemente real aunque `state.units` estuviera vacío | PASA defensa técnica · S1 · PARCIALMENTE FUNCIONAL | Fallbacks de demostración del prototipo seguían conectados al flujo real de generación de sesiones y podían romper la trazabilidad Unidad→Actividad→Sesión | `prototype-data-guard-v41.js` V42 envuelve `fillSessionUnits()` para mostrar `Primero crea una unidad/proyecto` / `Sin actividad programada` cuando no hay unidades y envuelve `generateSession()` para bloquear la salida y explicar que no se usarán ejemplos internos; cuando existe una unidad real conserva el flujo original | Commit `433af65324eb29e36aaa43add768ac60a27ad816`; GitHub check `Vercel: success`; raíz pública HTTP 200; `/prototype-data-guard-v41.js` HTTP 200 y sirve V42 con los wrappers de `fillSessionUnits` y `generateSession` | Elimina una fuente concreta de contenido pedagógico inventado y mejora trazabilidad V3/V5. Sigue pendiente prueba física móvil y E2E real Unidad→Actividad→Sesión→Word; no demuestra aún exactitud curricular ni IA semántica real. |
| AUD-ROL-DATA-032 | Ficha Maestra / roles / integridad de datos | Primer ingreso o Ficha Maestra nunca guardada | No asumir si la persona es Docente, Director o cumple ambas funciones; exigir selección explícita y conservar únicamente una decisión ya guardada | `institution-master-v46.js` asignaba `Docente y Director` por defecto y, si el selector quedaba sin dato, `saveMaster()` volvía a imponer ese mismo rol; la app podía presentar una función no declarada por el usuario como si fuera un hecho | PASA tras corrección técnica · S2 · PARCIALMENTE FUNCIONAL | Un valor de conveniencia del prototipo se convirtió en dato maestro y podía propagarse a Docente/Director sin procedencia real | `institution-master-v46.js` V46.2 limpia solo el valor legado no guardado, añade `Selecciona tu función`, bloquea el guardado si falta rol y conserva intactos los roles ya confirmados en una Ficha Maestra con `updatedAt` | Commit `d65126e54a4b2f67cde4cf75d3913ad07840c9f4`; Vercel production READY para ese SHA; raíz HTTP 200; `/institution-master-v46.js` HTTP 200 y sirve V46.2 con selección explícita y validación | Mejora integridad y procedencia del rol, pero NO implementa autorización ni aislamiento. V5 mantiene PENDIENTES autenticación, permisos reales por rol, escalamiento de privilegios, bitácora y pruebas multiusuario. |
| AUD-SES-DATA-033 | Sesiones / campos vacíos / trazabilidad | Abrir una unidad real sin `activities`, con `activities=[]`, o con una actividad sin `area` o `title`, y pulsar “Preparar mi sesión” | No generar una sesión genérica ni completar silenciosamente área/título faltantes; mostrar que la unidad está incompleta y exigir una actividad programada válida | El flujo base podía llegar a `selectedActivity()`/`buildSession()` y usar fallbacks como `Área` y `Sesión de aprendizaje`, creando una sesión aparentemente válida a partir de una unidad real pero incompleta; además `loadUnitForSession()` asumía `unit.activities.map(...)` y podía romperse si `activities` no existía | PASA defensa técnica · S1 · PARCIALMENTE FUNCIONAL | La guardia V42 validaba que existiera una unidad, pero no la integridad mínima de su actividad fuente; el flujo legado tenía fallbacks silenciosos para área/título y asumía que `activities` siempre era un arreglo | `prototype-data-guard-v41.js` V43 valida unidad seleccionada y exige al menos una actividad con `area` y `title`; envuelve `loadUnitForSession()` para evitar `.map` sobre datos incompletos y bloquea `generateSession()` con mensaje claro en lugar de completar campos faltantes | Commit `fb1d49efb36b1e6f8c133df6e4dd54dd1e08dc56`; Vercel production READY para ese SHA; raíz HTTP 200; `/prototype-data-guard-v41.js` HTTP 200 y sirve V43 con `selectedRealUnit`, `validActivity` y bloqueo de unidad incompleta | Reduce alucinación/fallback silencioso y cubre prueba V5 de campos vacíos. Sigue pendiente prueba E2E física y casos de corrupción/persistencia real; no demuestra exactitud curricular ni reemplaza validación backend. |

## Bloqueantes V5 vigentes

Siguen pendientes, entre otros: autenticación y aislamiento multiusuario; backend/persistencia segura; restore real; IA semántica remota; pruebas físicas de móvil; Word/PDF/impresión reales; prueba de 100 generaciones; año completo; OWASP ASVS; privacidad; concurrencia; costo IA; pilotos reales; Carpeta Director y Registro Auxiliar productivos.

**Estado de lanzamiento: NO APROBADO.**