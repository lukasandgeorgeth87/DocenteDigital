/* DocenteDigital – flujo de elección v8: situaciones y productos basados en el sentido expresado por el docente */
(function(){
  if(window.__ddProposalChoiceV8)return;window.__ddProposalChoiceV8=true;
  const E=v=>escapeHtml(v), baseCreate=window.createUnitDemo; state.pendingUnitChoice=state.pendingUnitChoice||null;if(typeof save==='function')save();
  const tidy=s=>String(s||'').replace(/\s+/g,' ').trim();
  function meaningFor(raw){try{if(typeof window.ddUnderstandPlanningDescription==='function')return window.ddUnderstandPlanningDescription(raw)}catch(e){}return {raw,focus:raw||'la situación descrita',problem:'',cause:'',consequence:'',goal:'',opportunity:'',actors:[],place:'',confidence:0,status:'lectura preliminar',gaps:['falta precisar el sentido de la situación']};}
  function shortFocus(m,raw){const f=tidy(m?.focus||'');return f&&f!=='la realidad descrita'?f:tidy(raw).slice(0,140)||'la situación descrita';}
  function placeFor(m){if(tidy(m?.place))return tidy(m.place);if(typeof window.ddTerritorialPhrase==='function')return window.ddTerritorialPhrase();const c=state.teacherContext||{};return tidy(c.locationName||c.community||c.district)||'entorno de los estudiantes';}
  function challengeFor(m,focus){const problem=tidy(m?.problem),goal=tidy(m?.goal);if(problem&&goal)return `¿Cómo podemos comprender ${focus}, analizar la situación planteada y construir una respuesta sustentada que contribuya a ${goal}?`;if(problem)return `¿Qué necesitamos investigar y explicar sobre ${focus} para comprender mejor el problema planteado y sustentar una respuesta pertinente con evidencias?`;if(goal)return `¿Qué necesitamos comprender, investigar y poner en práctica sobre ${focus} para ${goal}, y cómo demostraremos que nuestra respuesta es pertinente?`;return `¿Qué necesitamos observar, preguntar e investigar sobre ${focus} para comprenderlo mejor y qué evidencias nos permitirán explicar lo aprendido?`;}
  function situationOptions(raw){const m=meaningFor(raw),focus=shortFocus(m,raw),grades=(state.grades||[]).join(', ')||'los grados seleccionados',place=placeFor(m),reto=challengeFor(m,focus),pieces=[];if(tidy(m.problem))pieces.push(`La descripción del docente plantea la siguiente necesidad o problema: ${tidy(m.problem)}.`);if(tidy(m.cause))pieces.push(`También se menciona como causa: ${tidy(m.cause)}.`);if(tidy(m.consequence))pieces.push(`Como consecuencia expresada se identifica: ${tidy(m.consequence)}.`);if(tidy(m.opportunity))pieces.push(`La descripción reconoce además esta oportunidad o fortaleza: ${tidy(m.opportunity)}.`);if(tidy(m.goal))pieces.push(`La intención expresada es ${tidy(m.goal)}.`);const evidence=pieces.length?pieces.join(' '):'La información disponible todavía no permite afirmar un problema, causa o finalidad específicos sin inventarlos; por ello, la propuesta se plantea como una indagación inicial y deberá precisarse con lo que observen y expresen los estudiantes.',intro=`En ${place}, los estudiantes de ${grades} se relacionan con una situación cercana descrita por el docente en torno a ${focus}. ${evidence}`,a=`${intro} Para darle sentido al aprendizaje, se propone recuperar lo que los estudiantes ya saben, contrastar información, formular preguntas y movilizar las competencias necesarias para comprender la situación con mayor profundidad. Reto: ${reto}`,b=`${intro} La experiencia se abordará desde las voces, observaciones y preguntas de los estudiantes, evitando asumir hechos que no hayan sido expresados o comprobados. A partir de evidencias y distintas perspectivas, deberán construir una explicación, decisión o respuesta acorde con lo que realmente encuentren. Reto: ${reto}`;return {meaning:m,focus,reto,situations:[{key:'A',title:'Opción A · Analítica y contextualizada',text:a},{key:'B',title:'Opción B · Vivencial e investigativa',text:b}]};}
  function productOptions(raw,pack){const m=pack.meaning,focus=pack.focus,hasAction=!!(tidy(m.problem)||tidy(m.goal));return [{key:'1',title:`Portafolio de evidencias sobre ${focus}`.slice(0,90),text:`Producción organizada con preguntas, registros, textos, representaciones, datos, explicaciones y conclusiones elaboradas durante la investigación de ${focus}.`},{key:'2',title:`Muestra explicativa: comprendemos ${focus}`.slice(0,90),text:`Presentación para un destinatario real en la que los estudiantes comuniquen lo comprendido sobre ${focus}, utilizando evidencias de las áreas seleccionadas y explicando cómo llegaron a sus conclusiones.`},hasAction?{key:'3',title:`Propuesta sustentada frente a ${focus}`.slice(0,90),text:`Propuesta, campaña, acuerdo o acción viable construida a partir de las evidencias obtenidas sobre ${focus}; debe responder al reto sin añadir problemas o soluciones que no hayan sido comprobados.`}:{key:'3',title:`Guía de preguntas y hallazgos sobre ${focus}`.slice(0,90),text:`Guía o mural colectivo que reúna lo que se sabía al inicio, las preguntas investigadas, los hallazgos, las fuentes consultadas y las nuevas explicaciones construidas sobre ${focus}.`}];}
  function dataFor(brief){let pack=situationOptions((brief||'').trim());pack={...pack,products:productOptions(brief,pack)};if(typeof window.ddApplyExpertReasoningToProposal==='function')pack=window.ddApplyExpertReasoningToProposal(brief,pack)||pack;return pack;}
  function ensureHost(){let host=byId('ddProposalChooser');if(!host){host=document.createElement('div');host.id='ddProposalChooser';host.className='dd-proposal-chooser hidden topgap';byId('unitPanel')?.appendChild(host);}return host;}
  const situationCard=x=>`<label class="dd-choice-card"><input type="radio" name="ddSituation" value="${x.key}"><span class="pill">${E(x.key)}</span><h3>${E(x.title)}</h3><p>${E(x.text)}</p><b class="dd-pick">○ Elegir esta propuesta</b></label>`,productCard=x=>`<label class="dd-choice-card"><input type="radio" name="ddProduct" value="${x.key}"><span class="pill">Producto ${E(x.key)}</span><h3>${E(x.title)}</h3><p>${E(x.text)}</p><b class="dd-pick">○ Elegir este producto</b></label>`;
  function wireSelection(name){document.querySelectorAll(`input[name="${name}"]`).forEach(r=>r.addEventListener('change',()=>document.querySelectorAll(`input[name="${name}"]`).forEach(x=>x.closest('.dd-choice-card')?.classList.toggle('selected',x.checked))));}
  function showSituations(brief,type){const d=dataFor(brief),host=ensureHost();host.classList.remove('hidden');state.pendingUnitChoice={brief,type,situations:d.situations,products:d.products,meaning:d.meaning,reto:d.reto,expertReasoning:d.expertReasoning||null,selectedSituation:null,selectedProduct:null};if(typeof save==='function')save();const warning=d.expertReasoning?`<div class="notice">🧠 <b>Lectura experta:</b> ${E(d.expertReasoning.interpretation)}</div>`:(d.meaning?.confidence<50?`<div class="notice">⚠️ <b>Interpretación preliminar:</b> faltan datos para afirmar con seguridad problema, causas, actores o finalidad. Las propuestas no los inventan y pueden ajustarse.</div>`:'');host.innerHTML=`<div class="dd-choice-intro"><span class="pill">PASO 2</span><h2>Elige la Situación Significativa</h2><p>Las propuestas parten únicamente de lo expresado en el contexto y del nivel seleccionado.</p>${warning}</div><div class="dd-choice-grid">${d.situations.map(situationCard).join('')}</div><label class="dd-own"><b>✍️ O usar mi propia Situación Significativa</b><textarea id="ddOwnSituation" placeholder="Pega o escribe aquí tu propia situación significativa..."></textarea></label><div class="actions"><button class="btn" id="ddContinueProducts">Continuar: ver 3 productos →</button><button class="btn ghost" id="ddCancelChoice">Cancelar</button></div>`;wireSelection('ddSituation');byId('ddCancelChoice').onclick=()=>host.classList.add('hidden');byId('ddContinueProducts').onclick=()=>{const own=(byId('ddOwnSituation')?.value||'').trim(),chosen=document.querySelector('input[name="ddSituation"]:checked')?.value;if(!own&&!chosen)return alert('Elige una situación o escribe la tuya.');state.pendingUnitChoice.selectedSituation=own||d.situations.find(x=>x.key===chosen)?.text;if(typeof save==='function')save();showProducts(d,host);};host.scrollIntoView({behavior:'smooth',block:'start'});}
  function showProducts(d,host){host.innerHTML=`<div class="dd-choice-intro"><span class="pill">PASO 3</span><h2>Elige el producto o evidencia integradora</h2><p>Las opciones se relacionan con el reto y el nivel, sin inventar una campaña, feria o problema.</p></div><div class="dd-product-grid">${d.products.map(productCard).join('')}</div><label class="dd-own"><b>✍️ O usar mi propio producto</b><textarea id="ddOwnProduct"></textarea></label><div class="actions"><button class="btn" id="ddBuildUnit">✓ Elegir y construir la unidad/proyecto</button><button class="btn ghost" id="ddBackSituation">← Volver a situaciones</button></div>`;wireSelection('ddProduct');byId('ddBackSituation').onclick=()=>showSituations(state.pendingUnitChoice.brief,state.pendingUnitChoice.type);byId('ddBuildUnit').onclick=()=>{const own=(byId('ddOwnProduct')?.value||'').trim(),chosen=document.querySelector('input[name="ddProduct"]:checked')?.value;if(!own&&!chosen)return alert('Elige uno de los 3 productos o escribe el tuyo.');const item=d.products.find(x=>x.key===chosen);state.pendingUnitChoice.selectedProduct=own||item?.text;state.pendingUnitChoice.selectedProductTitle=own?'Producto propuesto por el docente':(item?.title||'Producto final');if(typeof save==='function')save();finalizeChoice(host);};host.scrollIntoView({behavior:'smooth',block:'start'});}
  function finalizeChoice(host){const pending=state.pendingUnitChoice;if(!pending)return;const before=new Set((state.units||[]).map(u=>u.id));baseCreate();const unit=(state.units||[]).find(u=>!before.has(u.id))||state.units?.[0];if(!unit)return;unit.situation=pending.selectedSituation;unit.reto=pending.reto||unit.reto;unit.planningMeaning=pending.meaning||unit.planningMeaning;unit.expertReasoning=pending.expertReasoning||null;unit.selectedSituationSource=pending.situations.find(x=>x.text===pending.selectedSituation)?.key||'Docente';unit.situationOptions=pending.situations;unit.product=pending.selectedProduct;unit.productTitle=pending.selectedProductTitle;unit.productOptions=pending.products;unit.selectionApproved=true;if(typeof save==='function')save();renderUnits();renderUnitOutput(unit);fillSessionUnits();host.classList.add('hidden');byId('unitOutput')?.scrollIntoView({behavior:'smooth'});}
  window.createUnitDemo=function(){const type=byId('unitType')?.value||'Unidad de aprendizaje',brief=byId('unitSituation')?.value.trim()||'';if(!brief)return alert('Describe una idea, situación, necesidad, problema u oportunidad del contexto.');showSituations(brief,type);};
  const css=document.createElement('style');css.textContent=`.dd-proposal-chooser{border-top:2px dashed #8a9a90;padding-top:18px;margin-top:18px}.dd-choice-intro{background:#f5faf7;border:1px solid #d6e4dc;border-radius:14px;padding:14px 16px;margin-bottom:12px}.dd-choice-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.dd-product-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}.dd-choice-card{display:block;border:2px solid #d8dfdb;border-radius:14px;padding:14px;background:#fff;cursor:pointer}.dd-choice-card.selected{border-color:#2f7e5a;background:#ebf7f0}.dd-choice-card h3{margin:8px 0}.dd-choice-card p{line-height:1.45}.dd-pick{display:block;margin-top:10px;color:#286c4e}.dd-own{display:block;margin-top:14px}.dd-own textarea{width:100%;min-height:95px;margin-top:7px}.dd-proposal-chooser .actions{margin-top:14px}@media(max-width:850px){.dd-product-grid,.dd-choice-grid{grid-template-columns:1fr}}`;document.head.appendChild(css);
})();